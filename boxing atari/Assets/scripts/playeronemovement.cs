using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playeronemovement : MonoBehaviour
{
/*
    //public GameObject gameOver;


    //public Rigidbody2D rb1;

    //float horizontal;
    //float vertical;

    //public float moveSpeed = 3f;
    //public float invokeTime = 5f;

    //public GameObject upJab;
    //public GameObject downJab;

    //bool jabbing1 = false;
    //bool jabbing2 = false;

    //public GameObject upbox;
    //public GameObject downbox;

    private void Start()
    {
        //rb1 = GetComponent<Rigidbody2D>();
    }

    void returnUpJab()
    {
       // upJab.transform.localScale = new Vector3(1, 0.46f, 1);

        //jabbing1 = false;

    }
    void returnDownJab()
    {
       // downJab.transform.localScale = new Vector3(1, 0.46f, 1);

       // jabbing2 = false;
    }

        void Update()
        { 
        
        if (GameOver.Instance.over == true)
        {



            horizontal = Input.GetAxisRaw("Horizontal");
            vertical = Input.GetAxisRaw("Vertical");


            if (Input.GetKeyDown("q") && jabbing1 == false)
            {
                upJab.transform.localScale = new Vector3(1.06f, 0.46f, 1);
                jabbing1 = true;
                Invoke("returnUpJab", invokeTime);
            }

            if (Input.GetKeyDown("e") && jabbing2 == false)
            {
                downJab.transform.localScale = new Vector3(1.06f, 0.46f, 1);
                jabbing2 = true;
                Invoke("returnDownJab", invokeTime);
            }
        

            if (punch.Instance.jabbing1 == true)
            {
                punch.Instance.upbox.SetActive(true);
            }
            else
            {
                punch.Instance.upbox.SetActive(false);
            }

            if (punch.Instance.jabbing2 == true)
            {
                punch.Instance.downbox.SetActive(true);
            }
            else
            {
                punch.Instance.downbox.SetActive(false);
            }
        }
       */ 
    
          
}
    

    

